apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-data
  namespace: bankapp-namespace
spec:
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  
